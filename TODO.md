# TODO - Потенциальные улучшения

## Реализовано ✅

- [x] **#1 Confirmation Delay** - 3 тика подтверждения перед входом
- [x] **#2 Trend Alignment** - Short vs Long VWAP, вход только по тренду  
- [x] **#3 Trade Cooldown** - 30 сек после закрытия позиции

---

## Осталось сделать 📋

### #4 Volume Filter (Средний приоритет)
Требовать минимальный объём в последних тиках:
```rust
let recent_volume: Decimal = ticks.iter().rev().take(10).map(|t| t.size).sum();
if recent_volume < min_volume_threshold {
    return; // Пропускаем - недостаточно объёма
}
```
**Эффект**: Входим только при реальной активности, игнорируем шум.

---

### #5 Scanner Stability (Средний приоритет)
Увеличить стабильность Scanner:
- Увеличить `score_threshold_multiplier` до 1.5-2.0
- Добавить `min_symbol_hold_time: 300` (5 минут минимум на символе)

```rust
// Не переключать если на текущем символе < 5 минут
if symbol_switch_time.elapsed() < Duration::from_secs(300) {
    return; // Слишком рано для переключения
}
```
**Эффект**: Меньше прерванных сделок из-за частого переключения.

---

### #6 Adaptive Momentum Threshold (Высокая сложность)
Адаптировать threshold под текущую волатильность:
```rust
let avg_move = calculate_recent_volatility(); // ATR-подобный расчёт
let adaptive_threshold = avg_move * 0.5; // 50% от средней волатильности
```
**Эффект**: Threshold подстраивается: выше на волатильных, ниже на спокойных монетах.

---

## Параметры для тюнинга

| Параметр | Текущее | Диапазон |
|----------|---------|----------|
| `confirmation_count` | 3 | 2-5 |
| `trade_cooldown_secs` | 30 | 15-60 |
| `momentum_threshold` | 0.001 (0.1%) | 0.0005-0.003 |
| `score_threshold_multiplier` | 1.2 | 1.2-2.0 |

---

*Последнее обновление: 2026-01-21*
